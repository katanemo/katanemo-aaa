{
  "name": "katanemo-ehr-server-lamba-authorizer",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "postinstall": "(cd lambda/auth && npm install && cd ../diagnostic && npm install && cd ../patients && npm install);",
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk"
  },
  "author": {
    "name": "Adil Hafeez <adil@katanemo.com>"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "*",
    "aws-cdk": "*",
    "esbuild": "*",
    "typescript": "~4.6.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.0.0",
    "constructs": "^10.0.0"
  }
}
